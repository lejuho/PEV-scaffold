# <PROJECT> — 기능 명세서

> 이 파일은 Hermes flow의 **완료 판정 원장**이다. AGENTS.md "Project Completion
> (Spec Exhaustion)" 참조. 아래 두 마커만 flow가 집계한다.
>
> - `<!-- SPEC-REQ -->` : 뒤따르는 체크박스는 **구현 대상**. 구현되면 `[x]`.
> - `<!-- SPEC-DECISION -->` : 뒤따르는 체크박스는 **사람이 내릴 결정**. 코드로 닫지 않는다.
>
> 마커 스코프는 다음 마커 또는 다음 heading(`#`)까지. 마커 밖 체크박스는 무시된다.
> flow는 SPEC-REQ가 전부 `[x]`가 되면 정지한다 — 열린 SPEC-DECISION이 있으면 사용자에게
> 결정을 요청하고(`needs_decision`), 없으면 완료로 본다(`spec_complete`).

## 0. 시스템 개요

<한 문단으로 무엇을 만드는지>

---

## 1. <모듈명>

### 1.1 유저 스토리

<누가, 무엇을, 왜>

### 1.2 기능 요구사항

<!-- SPEC-REQ -->
- [ ] <구현할 기능 단위 1>
- [ ] <구현할 기능 단위 2>

### 1.3 ❓ 미정 사항

<!-- SPEC-DECISION -->
- [ ] <사용자가 확정해야 하는 결정 1>
- [ ] <사용자가 확정해야 하는 결정 2>

---

<!-- 모듈마다 위 구조(기능 요구사항 = SPEC-REQ, 미정 사항 = SPEC-DECISION)를 반복 -->
